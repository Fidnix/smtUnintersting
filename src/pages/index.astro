---
// const cookie = Astro.request.headers.get("cookie");
// function isLoggedIn(cookie: string | null) {
//   return cookie && cookie.includes("loggedIn=true");
// }

// return !isLoggedIn(cookie)
//   ? Astro.redirect("/login")
//   : Astro.redirect("/simulador");

return Astro.redirect("/simulador");
---
